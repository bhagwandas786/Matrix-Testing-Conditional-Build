{
  "name": "github-actions-assignment",
  "version": "1.0.0",
  "scripts": {
    "lint": "echo 'Linting would check code here' || echo 'Linting completed'",
    "test": "mkdir -p test-results && jest --json --outputFile=test-results/results.json"
  },
  "devDependencies": {
    "jest": "^30.1.3"
  }
}
